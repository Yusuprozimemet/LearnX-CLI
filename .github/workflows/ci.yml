name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main", "v*-*"]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install "ruff>=0.4"

      - name: Check style
        run: ruff check tutor/

      - name: Check formatting
        run: ruff format --check tutor/

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    if: true  # change to false to skip unit tests
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          grep -v 'audioop-lts' tutor/requirements.txt > /tmp/requirements-filtered.txt
          pip install -r /tmp/requirements-filtered.txt
          pip install -r tutor/requirements-dev.txt

      - name: Run unit tests
        run: python -m pytest tutor/tests/ --ignore=tutor/tests/e2e/ -v

  e2e-tests:
    name: E2E Smoke Tests
    runs-on: ubuntu-latest
    needs: unit-tests
    if: true  # change to false to skip e2e tests
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Python dependencies
        run: |
          grep -v 'audioop-lts' tutor/requirements.txt > /tmp/requirements-filtered.txt
          pip install -r /tmp/requirements-filtered.txt
          pip install -r tutor/requirements-dev.txt

      - name: Install Playwright browsers
        run: python -m playwright install chromium --with-deps

      - name: Run E2E smoke tests
        run: python -m pytest tutor/tests/e2e/ -v
        env:
          # LLM calls are mocked in conftest.py — no real key needed
          ANTHROPIC_API_KEY: "test-key-not-used"
